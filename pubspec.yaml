name: meshagent_agents
description: Agent chat sessions and clients for Meshagent Dart applications.
homepage: https://www.meshagent.com
version: 0.40.2

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  meshagent: ^0.40.2
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
